## Packages
framer-motion | Smooth entry animations for the UI and results

## Notes
Backend handles in-memory identity reconciliation
API endpoint is POST /identify
Form requires at least one of: email or phoneNumber
